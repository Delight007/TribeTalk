// src/config/firebaseNativeSetup.ts
import firebase from '@react-native-firebase/app';

// No manual initializeApp if using native config files.
// You can export firebase for other modules:
export default firebase;
